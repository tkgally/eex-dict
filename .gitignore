# The generated site is built by GitHub Actions on every merge to main and is never committed.
docs/
# Python
__pycache__/
*.pyc
.pytest_cache/
# Run-time scratch (downloaded resources, model raw responses that are not part of a record)
.tmp/
.cache/
# Playwright and node artefacts
node_modules/
playwright-report/
test-results/
