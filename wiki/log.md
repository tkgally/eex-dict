# Log

*Append-only, newest first. Header: `## [YYYY-MM-DD] <operation> | <title>`. At most 200 words per entry.*

## [2026-09-19] originality | First originality check: 10 sampled, 3 rewritten

Seventh scheduled run, forced (run 10 is a multiple of `originality_every_runs`). Pre-flight clean. `originality_check.py sample --n 10` over the 132 reviewed entries; an exact-phrase web search per row plus the reviewer question to reviewer-a (one combined call). No search matched a published source for any of the 10, but reviewer-a called all 10 "copied" on style alone, no source cited — logged as reviewer overcall in new page `wiki/notes/reviewer-noise.md`. On my own reading against recalled published wording, three looked like real paraphrases of a specific dictionary's sentence shape rather than generic convergence: `be-v` `be-that-as-it-may`, `please-interj` sense 3 ("begging"), `person-n` `in-person`. Rewrote all three; full pipeline on each: 17 issues, 7 blocking, 9 applied/8 rejected. Two first-draft rewrites hit defining-vocabulary violations (`admit`, `mail`, `begging`, `plea`) and were rewritten again. Also cleared `person-n`'s long-standing `markup-pending` flag while open (6 minor markup issues, applied), rejected a garbled British-vowel objection, and rejected dropping `pleasant-adj` from `please-interj`'s word family (shared Latin root). Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.16.

## [2026-09-19] build | Twelve pronouns: anyone, anything, both, each, either, enough, everybody, everyone, everything, few, he, her

Sixth scheduled run, build chosen by scheduler debt. Pre-flight clean: no open pull request, no orphan branches, inbox empty. Drafted 12 band-1 pronouns continuing the determiner/pronoun pos-split set. Pipeline: validate, inflect, pronounce-check (24 transcriptions, all verified). Two-reviewer panel: 45 issues, 30 blocking; 30 applied, 15 rejected. Real catches: `each-pron`/`either-pron` definitions had comma splices or grammar notes folded in (a pattern across five entries — moved to the explanation); `each-pron`'s own definition used the headword circularly; its "ten dollars each"/"two cookies each" examples were postposed-adverbial, not pronoun, uses, and were removed; `everything-pron`/`anything-pron` definitions restated the headword as `any`/`every` plus `thing`; three learner-error and one collocation item illustrated the determiner use and were replaced. Rejected: three pronunciation objections against `anyone`, `everybody`, contradicted by the CMU/panel agreement already recorded; a length-mark objection on `few`'s American IPA against house style; two usage-note objections that would have contradicted the published, reviewed `both-det`/`few-det` entries. Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.28.

## [2026-09-19] build | Ten determiners and four pronouns

Fifth scheduled run, chosen by scheduler debt (build highest). Pre-flight clean: no open pull request, no orphan branches, inbox empty. Claimed 20 slugs from the queue, then trimmed the batch to 14 (fewer, better entries) before drafting: determiners *that, their, these, this, those, what, whatever, which, whose, your* and pronouns *all, another, any, anybody*. Full pipeline run: validate, inflect (all verified), pronounce-check (28 transcriptions, all verified), two-reviewer panel (38 issues, 28 blocking). Adjudicated every blocking issue: 28 applied, 10 rejected. The two largest rejections were considered ones — kept *which one* and the *in which case / by which time* sense of `which-det` as determiner use (checked against a grammar reference: *which* directly precedes a noun there, `one`/`case`/`time`), against one reviewer's repeated claim that these are pronoun uses. Real catches: `whatever-det` and `all-pron` had drifted into their sibling pos-split entries' territory (clause use, noun-object "of" phrases) and were rewritten to stay noun-only / personal-pronoun-only respectively. Queued `these-pron` and `those-pron`, missing pos-split targets the existing queue rows for the set had not covered. Gate, caps, links, 292 tests, lint_vocab and crossref gates all pass. Spend: US$0.33.

## [2026-09-19] lint | Reviewer-precision switch-off; branch and claim cleanup

Fourth scheduled run, forced by the five-runs-since-last-lint rule. Pre-flight: no open pull request; two closed-by-owner branches show file residue against current `main` only because later runs kept editing the same entries — both already logged as owner-closed and absorbed by a different path, so no new curator lines; the other orphan branches confirmed fully absorbed (already logged). `claim.py --prune` removed four stale claim files.

`metrics.py --precision` over all 798 logged decisions: reviewer-a 0.56 overall, reviewer-b 0.37. Five (role, family) pairs stayed under 30 percent over twenty-plus decisions — reviewer-a `example-policy` (0.16/44), `grammar-code` (0.27/75), `sense-structure` (0.29/48); reviewer-b `example-policy` (0.05/83), `explanation` (0.24/21) — so all five are switched off in `tools/review_panel.py` (`DISABLED_FAMILIES`, three unit tests added): a flagged issue from these pairs is downgraded to `ok` before adjudication. Table and reasoning in [reviewer-precision](notes/reviewer-precision.md).

Mechanical checks: caps, links, `crossref --all --apply` (865 warnings, all expected missing closure targets), `lint_vocab --all --queue` (0 violations; noted two defining-vocabulary first senses using out-of-vocabulary words, `most-det`/`other-det`, left for a future review pass). Wiki index checked against pages on disk: no orphans, no dead links. Gate, caps, links, 292 unit tests pass. Spend: US$0.

## [2026-09-19] review | Draft entries reviewed; markup pending cleared on eight

Third scheduled run. Pre-flight: no open pull request; a merged branch confirmed absorbed and logged. Chose review mode. The four entries still draft since the founding session (can, child, each other, explain) got their missing reviewer-a pass and were promoted to reviewed; can gained a missing sense (impossibility, can't be true) and a corrected could/was-able-to contrast. Eight markup-pending entries (favorite, information, main, police, scissors, advice, afraid, asleep) got a second panel round, hand-added inline marks, and lost the flag; information's help-desk use moved from a miscoded subsense to its own countable sense, and asleep's etymology was corrected. Several reviewer-a objections (pronunciation, a grammar code) were rejected as wrong. 108 decisions logged, 101 applied. Reviewer precision: 0.875 / 0.944. Spend: US$0.33. 34 markup-pending entries remain; lint is due next run.

## [2026-09-18] build | Fourteen determiners: least, less, little, many, more, most, much, my, neither, no, other, our, several, such

Second scheduled Routine run. Pre-flight: no open pull request; one previously unlogged orphan branch (`claude/admiring-rubin-05mkcp`), fully absorbed, no residue, logged. `next_mode.py` chose build again. Claimed and drafted fourteen band-1 determiners, closing most of the core quantifier and possessive set. Both reviewers repeatedly caught one real problem: an "of" example or explanation (*several of the guests*, *many of the students*) mixing the pronoun/fused-head use into the determiner entry, against [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md); fixed throughout. Two misplaced idioms moved by the keyword rule: *once more* dropped (adverbial), *more or less* moved from **less** to **more**. Non-standard or incomplete collocation items were trimmed or completed (*no small* → *no small task*). Two double-negative usage notes were sharpened from "avoided" to "not standard English". Single-reviewer claims that would have broken the established comparative/superlative pattern across **less/least/more/most**, repeated the accepted **few**/**a few** ↔ **little**/**a little** structure, or relitigated the *either...or* / *at least* placement precedent were rejected with those reasons (109 decisions). **several**'s pronunciation stayed `disputed`; American settled by CMU, British flagged to the curator. Reviewer precision: 0.63 / 0.77. Spend: US$0.36.

## [2026-09-18] build | Ten determiners: both, each, either, enough, every, few, half, her, his, its

First scheduled Routine run. Pre-flight: `origin/main` had already absorbed the prior session's work; pull request #4's branch closed by the owner without an API merge but fully absorbed with no residue, logged in `reviews/needs_curator.txt`. Claimed and drafted ten determiners, band 1. Both reviewers caught real problems: several entries mixed the pronoun/adverb use of the same word into the determiner's examples and collocations (*each of us*, *either of them*), which [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md) reserves for a separate entry; American IPA carried stray length marks in three entries; two adaptation notes gave raw IPA instead of a plain respelling; a usage note asserted a false rule about **both** and a negative verb. Fixed and logged (59 decisions); a few single-reviewer, uncorroborated restructuring claims on **half** were rejected. *her*'s American pronunciation stayed `disputed`, siding with CMU over the panel; flagged. Nine pos-split pointers moved from `word_family` to `see_also` per the style guide, and queued. All ten pass the gate. Reviewer precision: 0.73 / 0.78. Spend: US$0.26.

## [2026-09-18] decision | Seed-set feedback: inline marks, part-of-speech discipline, plain pronunciation notes

Processed the owner's feedback on the seed set (`inbox/archive/20260918_feedback_on_seed_set.txt`), the first item to arrive in `inbox/`. Two decision pages: [inline-markup](decisions/inline-markup.md) (`**word**` for a word named as a word, `*phrase*` for a quoted illustration, the headword in examples marked by the site at build time) and [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md) (one part of speech per entry, the entry agrees with itself, origin claims only in etymology, pronunciation notes in plain words). Tools: `eexlib`, `link_words`, `build_site`, `validate` (mark rules, an example without its headword, pronunciation jargon), `lint_vocab`, `originality_check`, `review_panel` (marks explained; a `consistency` verdict); 15 new unit tests; style guide section 6 and rules in sections 4, 7, 11, 12, 16; conventions; the Routine's review mode. Entries: 40 marked up by hand (the model entries, the function words, the drafts, the three the feedback named); 42 flagged `markup-pending` (a new flag value) for the review runs; *another*, *money*, *all*, *alone*, *favorite* corrected; seven pronunciation notes rewritten; *another* pron and *alone* adv queued. Open questions 4 (split *be*, *have*, *do*, *one*?) and 5 (the look of the marks). Site rebuilt, browser check 54 of 54. Spend: US$0.

## [2026-09-17] build | Founding session, Stages C and D: the seed set reviewed and published, the Routine dry-run

Both reviewers read all 82 seed entries (77 seed plus five determiners from the Routine dry run); the four smallest (*can*, *child*, *each other*, *explain*) got only one reviewer before the day's budget cap stopped the panel and stay `draft`. The session adjudicated 787 issues in 798 logged decisions (367 applied, 431 rejected, none escalated) and fixed the entries; 78 are `reviewed`. Reviewer precision measured for the first time ([note](notes/reviewer-precision.md)): 0.53 and 0.21, with three families below the switch-off line. The reviewer prompt's list of house conventions was extended; the style guide gained two rules (subsense restrictions live in the explanation; hedged claims about other languages). The site was built from the reviewed entries and passed the browser check (54 checks). A Sonnet-class dry run of `routine-prompt.md` drafted the five determiners and led to prompt fixes ([note](notes/routine-dry-run-v1.md)). Queue: 82 done, 4,195 pending. Spend on 2026-09-17: US$4.99 of 5.00, all reviews.

## [2026-09-16] setup | Founding session, Stage B: word lists, pronunciation test, pipeline and site tools

Built `schema/defining-vocabulary.txt` (2,394 lemmas) from three models' independent proposals in 37 semantic-field chunks, the intersection as the core, 682 single-vote words accepted one by one, gaps filled; `headwords/queue.tsv` with 3,907 rows (band 1 from the list, bands 2 and 3 by the same panel's median). Ran the pre-registered pronunciation test on 500 words and four models; adopted the two-vote rule (verified precision 95.0 percent) and amended the normalization for five notation conventions ([note](notes/pronunciation-model-test-v1.md), [decision](decisions/pronunciation-pipeline.md)). Wrote the exceptions table (checked by two models, [note](notes/inflection-exceptions-check.md)) and the tools `inflect`, `lint_vocab` (with a transparent-derivative rule), `pronounce_check`, `review_panel`, `crossref`, `queue`, `claim`, `next_mode`, `metrics`, `absorb_branch`, `originality_check`, `link_words`, `build_site`, `site_check`; `routine-prompt.md`; `tools/README.md`; the Pages workflow. Seed entries are drafted locally (77) and follow in Stage C. Spend so far today: US$4.75 of 5.00.

## [2026-09-16] setup | Founding session, Stage A: scaffold

Created the repository skeleton from the founding prompt: `framework.md` copied verbatim from the owner's knowledge-base framework; `PROJECT.md` (charter), `CLAUDE.md`, `resume-prompt.md`, `NEXT.md`, licences (data CC0 1.0, code MIT), README; `schema/entry.schema.json` and `schema/vocabularies.json` with the original usage-label sets; `wiki/` with conventions, index, this log, open questions, and 28 decision pages (two of them, defining-vocabulary and pronunciation-pipeline, to be completed in Stage B); `inbox/`, `journal/`, `reviews/`, `headwords/`, `metrics/`, `config/` (budget rule, ledger, model roles); tools `validate.py`, `schema_check.py`, `eexlib.py`, `entry_path.py`, `check_caps.py`, `check_links.py`, `spend.py`, `openrouter.py`, `wait.py` with unit tests; `.github/workflows/validate.yml`; a first version of `wiki/style-guide.md` (model entries to come from the seed set). Verified reachable and recorded in `sources/README.md`: OpenRouter's model list, the CMU Pronouncing Dictionary raw file, the Wiktionary API. Model roles chosen and verified against the OpenRouter list (`config/models.md`).
