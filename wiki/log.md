# Log

*Append-only, newest first. Header: `## [YYYY-MM-DD] <operation> | <title>`. At most 200 words per entry.*

## [2026-09-22] build | Thirteen function-word entries: conjunctions and modals

Thirty-second scheduled run, build (highest scheduler debt). Pre-flight clean: no open pull request, no orphan branch, inbox empty. Claimed the top of the queue, exactly the remaining conjunctions and modals: *when, where, whether, while, yet* (conjunction) and *could, may, might, ought, shall, should, will, would* (modal) — two closed grammatical categories completed in one run. Inflections: all `source: none` as expected. Pronunciation: 26/26 transcriptions verified by full panel agreement. Panel: 72 issues, 40 blocking, 64 applied, 5 rejected (precision reviewer-a 0.929, reviewer-b 0.926). Recurring findings: absolute grammar claims needing softened exceptions (*will*/*would* in *if*/*when* clauses for willingness), an overclaimed "formal" register on *ought* and one sense of *might*, and an etymological aside in two usage notes that belonged in `etymology` instead — removed rather than sourced, since neither was verified against two open references. Rejected: `polite` challenged as a non-standard register label (it is a valid closed-vocabulary value) and a claim that a variant-listed contraction (*shan't*) needed hand-marking (the site auto-marks `variants[]`). Fixed twelve defining-vocabulary violations by rewording. 11 closure/crossref targets queued. Gate, caps, links, 305 tests, lint_vocab and crossref gates pass. Spend: US$0.47.

## [2026-09-22] site | Rebuild and browser check, no content changes

Thirty-first scheduled Routine run. Pre-flight: no open pull request, no orphan branch, inbox empty; checkout already at `origin/main`'s latest commit (the prior run's build had just merged). Selector picked **site** as highest-debt mode. `build_site.py` regenerated `docs/` from all 273 reviewed entries (247 headword pages, 0 drafts withheld, 0 redirects, 568 files) with no errors. `site_check.py` ran the full Playwright suite at phone (360x780) and desktop (1280x900) widths plus a dark-mode check: 54 of 54 checks passed — no JavaScript errors, no horizontal overflow, viewport meta and AI-authorship disclosure on every sampled page, search resolving an inflected form (*ran* → *run*) with Enter opening it, hover/tap preview on a linked word, and the translator's-view toggle showing, persisting through reload, and hiding again. No template or entry content required a fix. `docs/` was not committed, per rule. Spend: US$0.

## [2026-09-21] build | Eleven band-1 entries, act through alarm

Thirtieth scheduled run, build. Pre-flight clean: no open pull request, no orphan branch, inbox empty. `claim.py --from-queue` first grabbed the entire remaining band-1 preposition set, including *on* and *over*, which `NEXT.md` had flagged as the heaviest words still deferred; discarded that claim and hand-picked a diversified batch instead, per `NEXT.md`'s own suggestion: *act* (noun and verb), *activity, address, adjective, advertise, affect, aim, air, airport, alarm*. Panel: 38 issues, 18 blocking, 35 applied, 3 rejected (precision reviewer-a 0.905, reviewer-b 0.941); two reviewer-b parse failures (*activity-n*, *alarm-n*) re-ran cleanly. Rejected two reviewer-a label complaints on *adjective-n* and one reviewer-b markup complaint (both cited style-guide rules the reviewer had misread). Fixed seven defining-vocabulary violations (*opera, behave, identifies, aircraft, broadcast*) by rewording rather than adding entries. *affect-v*'s usage note wrongly denied the specialized psychology noun sense; narrowed the claim and queued *affect-n*. A stray Python list-mutation bug briefly spawned a runaway process while patching *alarm-n*'s collocations; killed it before it wrote anything, confirmed the file was untouched, and redid the fix correctly. 28 closure/crossref targets queued. Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$0.35 this run, US$3.93 today.

## [2026-09-21] lint | Sixth pass: mechanical checks clean, a tooling gap logged, no new switch-offs

Twenty-ninth scheduled run, forced (five runs since the last lint, one an inflated duplicate — see below). Pre-flight clean: no open pull request, no orphan branches, inbox empty. Caps and links green. `crossref.py --all --apply`: 797 missing targets, all expected closure gaps; 6 entries' `word_family` back-filled with mirrored back-links (`actor-n`, `adopt-v`, `age-v`, `ahead-adv`, `aware-adj`, `have-v`). `lint_vocab.py --all --queue`: 0 violations. `claim.py --prune` removed 2 stale claim files.

`metrics.py --precision`: no family newly crosses under 30 percent at twenty-plus decisions; the five already off stay off ([reviewer-precision](notes/reviewer-precision.md)). `reviewer-a` overall now 0.67 (1347, up from 0.64); `reviewer-b` 0.65 (489, up from 0.62). `reviewer-a` `pronunciation` moved toward the 30 percent line for the first time (0.33, was 0.37) — watch next pass.

Wiki check: index matches pages on disk; curator file has ten open items, no duplicates. Found a tooling gap while checking the log for gaps: `metrics/history.jsonl` has five duplicate rows (same `run_id`, always `build`), inflating `next_mode.py`'s run counts enough to trigger this lint a run early. Logged, not fixed, in [metrics-duplicate-calls](notes/metrics-duplicate-calls.md). Gate, caps, links, 305 tests, lint_vocab and crossref gates pass. Spend: US$0.

## [2026-09-21] build | Twelve band-1 entries, ability through ahead

Twenty-eighth scheduled run, build. Pre-flight clean. Diversified further into the band-1 backlog: *ability, accent, absorb, admire, active, alive, actor, adopt, adult* (adjective)*, age* (verb)*, agreement, ahead*. Panel: 42 issues, 24 blocking, 32 applied, 10 rejected (precision reviewer-a 0.812, reviewer-b 0.731); one reviewer-b parse failure on *actor-n* re-ran cleanly. Two entries were restructured rather than patched: *active-adj*'s "in use" sense conflated volcano, membership, and technical uses in one definition, so it split into in-use, taking-part, and a volcano subsense; *age-v* conflated plain and causative aging, so it split into an intransitive and a transitive sense, folding the food-aging use into those. Fixed five defining-vocabulary violations (*extra, distinguish, emphasis, organization, further/advanced*). Rejected a claim that "adult" cannot take "often before another noun" (it is a valid code) and an incoherent British-pronunciation objection on *adult-adj* (same dispute as *adult-n*, cmudict agrees with the drafter). *absorb-v*'s transcription corrected *s* to *z* per cmudict; *admire-v*'s American schwa kept but flagged unverified. Tried, then reverted, an exceptions-table note for British "ageing": it broke an existing unit test. 14 closure/crossref targets queued. Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$0.38 this run, US$3.58 today.

## [2026-09-21] review | Second panel round on the twenty oldest seed-set entries

Twenty-seventh scheduled run, review (selector: highest debt; 204 reviewed-once entries, no drafts). Panel on the 20 oldest one-round entries (*the, an, a, hand, money, have, do, used-to, must, un-, however, -ness, very, o'clock, TV, anyway, ASAP, both, enough, every*): 74 issues, 53 blocking, 58 applied, 16 rejected (precision reviewer-a 0.797, reviewer-b 0.733). Structural fix, both reviewers agreed: *the*'s correlative "the more, the better" sense is an adverb, not a determiner; split out, queued *the-adv*, style guide's model-entry note updated. *very-adv* similarly lost "very own"/"very same" (adjective) from its sense 2. Fact-checked three learner-error entries on *-ness* (*curiousness, strongness, difficultness* are all real, if rare, dictionary words) and removed them; verified British "quite" and "could do with" by web search before editing. Logged a tooling gap: `inflect.py` can never record a rare plural (*monies*) for an uncountable noun. Rejected several reviewer-a claims contradicted by the entries' own examples or house convention (IPA stress placement, the *-ness* stress-neutral rule). Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$0.89 this run, US$3.20 today.

## [2026-09-21] build | Ten band-1 nouns, verbs, and adjectives

Twenty-sixth scheduled run, build. Pre-flight clean. Diversified into the band-1 noun/verb/adjective backlog per the last run's note, deferring *on*/*over*: *accident, action, advantage, age, agree, allow, afford, angry, aware, available*. Panel: 31 issues, 17 blocking, 30 applied, 2 rejected (precision reviewer-a 1.0, reviewer-b 0.857); one full reviewer-b parse failure on *allow-v* re-ran cleanly. Notable fixes: *age-n* gained a missing informal "a long time" sense (*for ages*) and a corrected *of age* phrase (legal adulthood, not one activity); *agree-v*'s bare transitive "agree a price" example was chiefly-British and unlabeled, so replaced with the universal "agree on" pattern instead of a region label; *advantage-n*/*age-n* first senses widened to countable-or-uncountable; *angry-adj*'s adaptation note wrongly called "angry at" American-only, removed. Rejected: reviewer-b's "usually passive" flag on *allow-v* (it is a valid closed-vocabulary pattern) and a stale core-idea nit on two-sense *age-n*. Three core_idea/definition fields used words with no entry yet (*history*, *permission*, *obtain*), reworded rather than left as violations. 14 closure/crossref targets queued. Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$0.29 this run, US$2.31 today.

## [2026-09-21] review | Second panel round on the twenty oldest one-round entries

Twenty-fifth scheduled run, review (selector: highest debt; no drafts, 213 entries reviewed once). Pre-flight clean. Panel on the 20 oldest one-round entries (*see, make, get, and, in-spite-of, of, big, old, run, take, go, because, it, one, some, actually, ago, give-up, look-after, well*): 121 issues, 80 blocking, 95 applied, 26 rejected (precision reviewer-a 0.76, reviewer-b 0.905). Two reviewer-b parse failures (`because-conj`, `actually-adv`) re-ran cleanly. Both reviewers flagged *one-num* mixing numeral and pronoun senses (one-part-of-speech rule): split into *one-num* and a new *one-pron* (replacing, generic, "a person" senses), full pipeline run on the new entry; *because* prep queued for its own future split (*because movies*). *it-pron*'s game sense ("you're it") moved to a phrase, matching *see-v*/*go-v* precedent for non-matching-POS fixed expressions set this run. Rejected two duplicate cases of reviewer-a confusing **one** with **won** (logged to `wiki/notes/reviewer-noise.md`) and five *see-v* phrase-style flags contradicting 40+ existing "used to" precedents. Fixed several raw-IPA and unhedged most/all-languages style violations found along the way. Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$1.55 this run, US$2.02 today.

## [2026-09-21] lint | Fifth pass: mechanical checks clean, no new switch-offs, no tooling gaps open

Twenty-fourth scheduled run, lint (forced: 5 runs since the last lint). Pre-flight clean: no open pull request, no orphan branch, inbox empty; checkout already at `origin/main`'s latest commit. Mechanical checks: `check_caps`, `check_links` clean; `crossref --all --apply` 0 errors, 928 warnings, all expected missing closure-queue targets, 5 entries got mirrored back-links; `lint_vocab --all --queue` 0 violations across 239 entries; `claim.py --prune` removed 3 stale claim files. `metrics.py --precision`: the five families switched off 2026-09-19 are unchanged (the downgrade-to-`ok` mechanism still holds); nothing newly crosses 30 percent at twenty-plus decisions. `reviewer-a` overall now 0.64 (was 0.62), `reviewer-b` 0.62 (was 0.58); `reviewer-a` `pronunciation` (0.37, 49) stays the nearest live family to the line. Recorded in [reviewer-precision](notes/reviewer-precision.md). The two tooling notes flagged "due a later run" (`pronounce-check-stale-flag`, `review-panel-decide-substring-collision`) already carry a 2026-09-20 fix, nothing outstanding. Wiki check: `index.md` matches all 30 decision pages and 11 notes on disk both ways; `log.md` has no gaps; `open-questions.md`'s five items and `needs_curator.txt`'s eight lines unchanged, no duplicates. No entries touched. Gate, caps, links, 305 tests, lint_vocab and crossref gates all pass. Spend: US$0.

## [2026-09-21] closure | Twelve words already used in the dictionary's own definitions

Twenty-third scheduled run, closure. Pre-flight clean: no open pull request, no orphan branch, inbox empty. Claimed 14 band-3 closure candidates; found half mistagged by `lint_vocab.py`'s morphological pos-guess (confident but unreliable on an -ing/-ed ending): declined *concerning-v* (duplicate of an existing *concerning-prep* row), *enclosed-v*, *opposed-v*, *surrounded-v*, *leaning-v* (real gap was base verb *lean*), *surrounding-v*, *arrangement-adj*, plus two stale rows (*honor-n*, *rank-n*, no longer in any entry) — requeued under the correct pos/lemma or dropped, logged in `wiki/notes/lint-vocab-pos-guess-gap.md`. Drafted 11 plus one editorial addition (*concerning*'s "worrying" adjective sense): *role, feature, contact, opposition, pressure, concerning* (prep and adj), *enclosed, opposed, surround, arrangement, lean*. Panel: 44 issues, 6 blocking after re-review, 44 applied, 8 rejected. *surround-v*'s `core_idea` drew four rounds of inconsistent complaints from one reviewer alone (reviewer-b never flagged it); rejected the last two rounds, logged to `wiki/notes/reviewer-noise.md`. *pressure-n* needed a second panel pass after a truncated reviewer-b reply (the 2026-09-20 parse-failure gate caught it). Senses added mid-review on legitimate findings: feature film/article, contact's touch sense, arrangement's placement sense, enclosed's letter sense. Gate, caps, links, 305 tests, lint_vocab and crossref pass. Spend: US$0.48.

## [2026-09-20] build | Three prepositions and ten core words

Twenty-second scheduled run, build (selector: highest scheduler debt). Pre-flight: previous run's pull request (#25) already merged; checkout reset cleanly to `origin/main`; no orphan branch, inbox empty. Diversified away from prepositions/pronouns (three straight runs) toward the much larger band-1 noun/verb/adjective backlog (1,042/403/345 pending): claimed *off, out, per* (closing three of the five remaining band-1 prepositions; *on, over* deferred for dedicated runs) plus *account, adult, add, accept, apologize, able, actual, anxious, awake, bad*. Panel: 35 issues, 21 blocking, 32 applied, 3 rejected (precision reviewer-a 0.95, reviewer-b 0.867). Notable: both reviewers flagged treating *out* as a preposition while three of four senses only occur as *out of*; kept the single-entry structure (matches major dictionaries) but removed the one adverbial example and tightened the wording. *actual-adj*'s false-friends note named specific languages, against the adaptation rule; fixed. *bad-adj* had two wrong attributive/predicative-only codes and an embedded "of food:" restriction inside a definition; fixed. Three pronunciation disputes (*per, adult, accept*-British) flagged and logged to the curator queue, CMU siding with the drafter on two. Four defining-vocabulary violations fixed by rewording. Gate, caps, links, 305 tests, lint_vocab and crossref gates pass. Spend: US$0.45 this run, US$2.18 today.

## [2026-09-20] build | Fifteen prepositions

Twenty-first scheduled run, build (selector: highest scheduler debt). Pre-flight clean: no open pull request, no orphan branch, inbox empty. Claimed 20 band-1 prepositions, trimmed to 15 for quality: *as well as, below, beside, beyond, despite, including, inside, like, minus, near, onto, outside, past, plus, since*; the other 5 (*off, on, out, over, per*) returned to the queue, not drafted. Panel: 53 issues, 37 blocking, 48 applied, 5 rejected (precision reviewer-a 0.87, reviewer-b 1.0). Notable catches: *plus* mixed a conjunction use into a preposition entry, restructured to one general-addition sense; *despite* was wrongly labelled formal by both reviewers; four entries (*inside, near, outside, onto*) called a standard American or British variant a learner error, all removed. Rejected: three "used to" wrapper requests for *near*'s plain phrasal definitions (matches `behind-prep`, `above-prep`) and one collocation removal for *since then* (patterns like *until then*). *beyond-prep*'s British pronunciation stayed disputed after two tries; flagged and logged to the curator queue. Three defining-vocabulary violations fixed by rewording. Gate, caps, links, 305 tests, lint_vocab and crossref gates pass. Spend: US$0.46 this run, US$1.73 today.

## [2026-09-20] review | Markup-pending backlog cleared: the last 15 entries

Twentieth scheduled run, review (no draft entries; selector picked review by scheduler debt). Pre-flight clean: no open pull request, no orphan branch, inbox empty, checkout already at `origin/main`. Of the 15 remaining `markup-pending` entries (the last of the 2026-09-18 inline-markup backlog), all 15 were open at once, each field checked by hand against `wiki/style-guide.md` section 6 and the already-marked model entries (`run-v`, `hand-n`, `the-det`) for precedent: `house-n`, `shit-n`, `bank-n`, `color-n`, `water-n`, `good-adj`, `thing-n`, `make-v`, `see-v`, `head-n`, `go-v`, `get-v`, `way-n`, `time-n`, `take-v`. No panel round needed (all already fully reviewed); the unit was hand markup only. Roughly 700 bold and 450 italic marks added across the batch, no wording changed, verified per entry against the pre-markup text. `markup-pending` cleared from all 15; zero entries carry the flag now. One pre-existing, unrelated validate warning surfaced on `time-n` (`kill-time`'s second example doesn't literally contain "time" or a listed form) — left as a content note, not a markup problem. Gate, caps, links, 305 tests, lint_vocab (0 violations) and crossref (0 errors, 728 expected closure-queue gaps) all pass. Spend: US$0 this run, US$1.26 today.

## [2026-09-20] lint | Fourth pass: mechanical checks clean, two tooling gaps fixed

Nineteenth scheduled run, lint (forced: 5 runs since the last lint). Pre-flight clean, checkout already at `origin/main`, no open pull request or orphan branch. Mechanical checks: `check_caps`, `check_links` clean; `crossref --all --apply` updated 6 entries (`except-prep, for-prep, none-pron, nothing-pron, pick-up-phrv, you-pron`) with missing back-links, 0 errors; `lint_vocab --all --queue` 0 violations; `claim.py --prune` removed 3 stale claim files. `metrics.py --precision`: the five families switched off 2026-09-19 stayed at their seed-set figures (mechanism confirmed working a fourth time); nothing newly crossed 30 percent at twenty-plus decisions; `reviewer-a` overall now 0.62 (1064), `reviewer-b` 0.58 (363), both up; watching `reviewer-a` `pronunciation` and `label` as they approach or recede from the threshold. `wiki/index.md` matches the filesystem both ways; `log.md` has no gaps; `open-questions.md`, `needs_curator.txt` unchanged, no duplicates. Fixed the two remaining logged tooling gaps: `review_panel.py --decide` gained `--index N` to disambiguate a quote nested inside another quote on the same field/role; `pronounce_check.py` now clears a stale `pronunciation-disputed` flag once every checked variety re-verifies, via a new `update_disputed_flag` helper. Eight new tests, 305 passing (was 297). No new entries; not this mode's unit. Spend: US$0.

## [2026-09-20] build | Three pronouns and ten prepositions

Eighteenth scheduled run, build. Pre-flight clean; inbox empty. Claimed 20 band-1 slugs, trimmed to 13 for quality: *nothing, other, what* (pronouns) and *behind, between, by, down, during, except, for, from, in, into* (prepositions); the other 7 (*as well as, below, beside, beyond, despite, including, inside*) returned to the queue, not drafted. Panel: 52 issues, 33 blocking, 50 applied, 2 rejected (precision reviewer-a 0.97, reviewer-b 0.95). Notable catches, both reviewers: *behind*'s "less advanced" sense had two adverbial (not prepositional) examples ("fell behind", "behind on rent"), removed; *except*'s "except for" was wrongly given its own idiom block with an invented conditional meaning belonging to *but for*; *into*'s "bump into" example showed a chance meeting, not a collision, moved to its own phrase entry; *for* and *in* each had a definition reusing the headword. Rejected: a pronunciation nitpick covered by a listed weak-form variant, and a markup call matching the style guide's own bold-phrase example. Fixed nine defining-vocabulary violations by rewording, found by a second lint pass after adjudication. Gate, caps, links, tests, lint_vocab and crossref gates pass. Spend: US$0.49 this run, US$1.26 today.

## [2026-09-20] build | Nine pronouns and three prepositions

Seventeenth scheduled run, build. Pre-flight clean: no open pull request, no orphan branch, inbox empty. Claimed 12 slugs: *none, some* (only two of the five "try one or two per run" quantifiers), *which, who, whom, whose, you, yours, yourself* (pronouns), and *as, at, before* (band-1 prepositions). Panel: 47 issues, 29 blocking, 43 applied, 4 rejected (precision reviewer-a 0.91, reviewer-b 0.93). Confirmed by both reviewers: *at*'s time sense circularly defined "at" with "at"; *before* had an awkward example and two adverbial collocations mismarked as prepositional; *you*'s generic sense was wrongly labelled informal. One reviewer alone caught that American usage prefers *that* over restrictive *which* and treats *whom* as more formal, not "strictly correct" (three entries shared this slip), and that *as*'s two senses were one sense wrongly split, with its *such as* phrase misplaced under the wrong keyword. Rejected an over-broad claim about uncountable-noun agreement after *none of*, and a non-issue flagging an accurate `core_idea`. Three vocabulary violations fixed by rewording (*target*, *rank*; one first-sense exemption kept). Gate, caps, links, 297 tests, lint_vocab and crossref gates pass. Spend: US$0.35 this run, US$0.78 today.

## [2026-09-20] originality | Second forced check: mixed reviewer verdicts, one rewrite

Sixteenth scheduled run (forced: run 20 is a multiple of 10). Pre-flight clean; inbox empty. Sampled 10 fields; searched each exact phrase (no match found); put the copied/original/cannot-tell question to reviewer-a. Unlike the first forced run's 100 percent "copied" with no source cited (`wiki/notes/reviewer-noise.md`), answers were mixed (4/4/2), still uncited; verdicts rested on the search plus the session's reading. Result: 5 original, 4 generic-overlap, 1 rewrite. The rewrite (`pick-up-phrv` sense 9, "continue") echoed a Macmillan synonym-list phrase found by search, unflagged by the reviewer; reworded and re-run through the full pipeline, which also caught eight genuine, unrelated accuracy problems in the same entry (an infinitive-style core idea, a wrong claim about sense 9, an imprecise synonym, a definition missing the wind/speed "increase" use, an overgeneralized adaptation note, a separability claim ignoring two intransitive senses) — all applied. Logged a gap: `--decide --quote` cannot disambiguate a quote nested inside another; worked around manually. Gate, caps, links, tests, lint_vocab, crossref pass. Spend: US$0.11 this run, US$0.43 of US$5.00 today.

## [2026-09-20] build | Two pronouns and eight prepositions

Fifteenth scheduled run, build. Pre-flight clean; pruned one stale remote-tracking ref. Claimed 10 slugs: *neither, nobody* (pronouns, only two of the seven "try one or two per run" quantifiers) and *about, above, across, after, against, along, among, around* (band-1 prepositions). Fixed a drafting slip before review: British *nobody* wrongly copied the American stress pattern; corrected and reverified. Panel: 41 issues, 27 blocking, 33 applied, 8 rejected (precision reviewer-a 0.76, reviewer-b 0.92). Real catches, several confirmed by both reviewers: *about* and *around* each wrongly folded an adverbial "approximately" sense (and, for *about*, an adjectival "be about to" phrase) into the preposition entry — removed and queued separately; *neither* overstated singular-only agreement; *around*'s definition circularly reused its signpost; misplaced collocations moved to the right sense. Rejected claims that *above ours* and *above all* lack an object (both have one), that *along with* permits plural agreement (a settled rule), and a region-label objection covered by the "mainly used in" fence. Six vocabulary violations fixed by rewording. Logged a gap: `pronounce_check.py` never clears a stale disputed flag after re-verification. Gate, caps, links, 297 tests, lint_vocab and crossref gates pass. Spend: US$0.32 of US$5.00.

## [2026-09-20] lint | Third pass: mechanical checks clean, three tooling gaps fixed

Fourteenth scheduled run, lint (forced: 5 runs since the last lint). Pre-flight clean, checkout already at `origin/main`. Mechanical checks: `check_caps`, `check_links` clean; `crossref --all --apply` updated 2 entries (`something-pron`, `themselves-pron`) with missing back-links, 0 errors; `lint_vocab --all --queue` 0 violations; `claim.py --prune` removed 3 stale claim files. `metrics.py --precision`: the five families switched off 2026-09-19 stayed at their seed-set figures; nothing newly crossed 30 percent. `wiki/index.md` matches the filesystem; `open-questions.md`, `needs_curator.txt` unchanged, no duplicates. Fixed the three logged tooling gaps due this pass: `review_panel.py --decide` now takes `--quote` to disambiguate two issues on one field/role, instead of silently colliding on the last verdict; `validate.py`'s `reviewed` gate now rejects a reviewer record that logged an error with zero verdicts; `lint_vocab.py`'s `pos_guess` checks a closed adjective-suffix list before defaulting to noun, and flags the remaining noun default in the queue row's note. Five new/updated tests, 297 passing (was 292). No new entries; not this mode's unit. Spend: US$0.

## [2026-09-19] review | Inline markup: eight seed-set entries converted

Thirteenth scheduled run, review (no draft entries, so the block was the 23 `markup-pending` entries from the 2026-09-18 inline-markup ruling). Pre-flight clean. No panel round needed (these entries already carry full reviews); the unit is hand markup of `wiki/style-guide.md` section 6 (`**word**` for a word or phrase named as a word, `*phrase*` for language quoted as an illustration), field by field, never by script. Converted the eight smallest entries by file size: `x-ray-n`, `record-v`, `record-n`, `news-n`, `people-n`, `family-n`, `big-adj`, `old-adj`. Method settled by comparing against the model entries and dozens of already-marked ones: a noun's "A/An X is..." core idea stays unmarked, but a verb's "To X is to..." and an adjective's or function word's "X means..." bold the headword; a compare note bolds only the introduced comparison word; illustrative usage, including placeholder patterns, is italicized even without a colon. Removed `markup-pending` from all eight. Validate, lint_vocab, crossref: 0 errors. Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Fifteen entries remain `markup-pending` (largest ones). Spend: US$0.

## [2026-09-19] closure | Six words the dictionary already uses

Twelfth scheduled run, closure. Pre-flight clean. Claimed 6 slugs: *addition, article, exception, identity, thumb* plus *additional*. Declined `additional-n`: `lint_vocab.py`'s pos-guesser had queued it as a noun when it is an adjective (logged, `wiki/notes/lint-vocab-pos-guess-gap.md`); requeued and claimed as `additional-adj`. Full pipeline on all six; 12 transcriptions, full agreement, none disputed. One reviewer-b parse failure on `addition-n` (known truncated-reply pattern); a re-run fixed it. Panel totals across rounds: 44 issues, 38 applied, 6 rejected (precision reviewer-a 0.90, reviewer-b 0.80). Real catches: two `exception-n` phrase definitions were fragments, not phrasal definitions; `article-n`'s grammar explanation overstated that articles are chosen noun by noun; `identity-n`'s countability was too narrow (recoded countable-or-uncountable). Rejected reviewer-a's repeated claim that *addition*'s "built onto a house" sense isn't mainly American, and reviewer-b's fabricated 40-word adaptation cap (house rule is 60). Six defining-vocabulary violations fixed by rewording definitions. Crossref queued 13 missing targets (*add-v*, *extra-adj*, *finger-n*, *identify-v*, others). Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.85 of US$5.00.

## [2026-09-19] build | Thirteen plural and demonstrative pronouns

Eleventh scheduled run, build. Pre-flight clean. Claimed 20 pronoun slugs, trimmed to 13: *we, us, they, them, theirs, themselves* (completes the plural sets), *someone, somebody, something*, and standalone *this, that, these, those*. Released *neither, nobody, none, nothing, other, some, what*, correctly resetting their queue status this time (a claim-release slip caught and fixed). Pipeline: validate, inflect, pronounce-check (`themselves` British disputed, flagged). Panel: 29 issues, 18 blocking, 24 applied, 4 rejected (precision 0.94/0.70). Real catches: singular *they/them* wrongly limited to unstated sex, missing the nonbinary use — broadened; *themselves* wrongly allowed a singular "thing" — fixed to plural; *that*'s definition overstated physical distance — broadened. Rejected a claim that *that* needs a relative-pronoun sense (filed at queued `that-conj`). Fixed three defining-vocabulary violations. Gate, caps, links, tests, lint_vocab and crossref gates pass. Spend: US$0.31.

## [2026-09-19] build | Thirteen personal and reflexive pronouns

Tenth scheduled run, build (highest scheduler debt). Pre-flight clean: no open pull request, no orphan branches, inbox empty. Claimed 20 pronoun slugs, trimmed to 13: subject/object/independent-possessive *I, me, she, him, his, hers, mine, ours* and reflexives *myself, himself, herself, itself, ourselves*; released *neither, nobody, none, nothing, other, some, somebody* to pending. Pipeline: validate, inflect (verified), pronounce-check (26 transcriptions; `ours`/`ourselves` American disputed, `ours` settled by CMU, `ourselves` flagged and sent to curator). Panel: 34 issues, 18 blocking, 30 applied, 4 rejected (precision 0.81/1.0). Real catch, repeated across four entries: a usage note claiming the reflexive-object restriction applies entrywide, contradicting each entry's own emphatic sense (*he himself doesn't believe it*) — rescoped. `itself-pron`'s claim that verbs like *open* never take a reflexive object contradicted its own examples (*turns itself off*); hedged. `she-pron`'s learner-error note contradicted its own ship/country sense; qualified. Fixed two defining-vocabulary violations and a missing British length mark on *me*. Queued *we-pron*, *us-pron*. Gate, caps, links, 292 tests, lint_vocab and crossref gates all pass. Spend: US$0.34.

## [2026-09-19] lint | Second lint pass: mechanical checks clean, stale curator lines resolved

Ninth scheduled run, forced (five runs since last lint). Pre-flight clean: no open pull request, no orphan branches (`list_branches` shows only `main` remotely), inbox empty. Caps and links green. `crossref.py --all --apply`: 723 missing targets, all expected closure-queue gaps; back-filled 6 entries' `word_family` with mirrored back-links (the *anybody/anyone/anything/everybody/everyone/everything*-pron set). `lint_vocab.py --all --queue`: 0 violations. `claim.py --prune` removed 2 stale claim files.

`metrics.py --precision`: no family newly crosses under 30 percent at twenty-plus decisions; the five already switched off stay off. `reviewer-a` overall now 0.57 (892 decisions), `reviewer-b` 0.49 (280, up from 0.37/222) — question 2 stays "keep". Watching `definition-style` (both roles) and `reviewer-a` `pronunciation`. Recorded in [reviewer-precision](notes/reviewer-precision.md).

Wiki check: index matches pages on disk both ways; log has no gaps; open questions has no stale items. `reviews/needs_curator.txt`: four prune-branch lines for branches confirmed already gone from the remote — moved to Resolved; the one open pronunciation question unchanged. Gate, caps, links, unit tests, lint_vocab and crossref gates all pass. Spend: US$0.

## [2026-09-19] review | Ten markup-pending entries re-reviewed and marked up

Eighth scheduled run, review chosen by scheduler debt (no drafts existed). Pre-flight clean: no open pull request, no orphan branches, inbox empty. Of 33 `markup-pending` entries (all also due a second panel round), took the 10 with fewest senses for a thorough pass in one run: `alone-adj`, `quickly-adv`, `sorry-adj`, `borrow-v`, `lend-v`, `speak-v`, `happy-adj`, `tell-v`, `talk-v`, `say-v`. Panel plus adjudication on each (86 decisions: 68 applied, 18 rejected), then hand-added the inline marks of style guide section 6 field by field and cleared the flag. Real catches: `alone-adj` sense 1 used adverbial examples (*lives alone*) as adjective ones, contradicting its own usage note; `quickly-adv` sense 2 was defined like *soon* (a point in time) instead of a duration; `say-v`'s parenthetical *say, six o'clock* use didn't fit "suppose" and became its own subsense; several entries had discrimination or adaptation claims true only "usually," not "never/only" (hedged per style rule). One tooling problem: `review_panel.py` silently recorded an empty, zero-verdict reviewer-b pass for `talk-v` on a truncated reply; re-run gave a normal review. Logged in new page `wiki/notes/review-panel-parse-failures.md`, no fix made. Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.61.

## [2026-09-19] originality | First originality check: 10 sampled, 3 rewritten

Seventh scheduled run, forced (run 10 is a multiple of `originality_every_runs`). Pre-flight clean. `originality_check.py sample --n 10` over the 132 reviewed entries; an exact-phrase web search per row plus the reviewer question to reviewer-a (one combined call). No search matched a published source for any of the 10, but reviewer-a called all 10 "copied" on style alone, no source cited — logged as reviewer overcall in new page `wiki/notes/reviewer-noise.md`. On my own reading against recalled published wording, three looked like real paraphrases of a specific dictionary's sentence shape rather than generic convergence: `be-v` `be-that-as-it-may`, `please-interj` sense 3 ("begging"), `person-n` `in-person`. Rewrote all three; full pipeline on each: 17 issues, 7 blocking, 9 applied/8 rejected. Two first-draft rewrites hit defining-vocabulary violations (`admit`, `mail`, `begging`, `plea`) and were rewritten again. Also cleared `person-n`'s long-standing `markup-pending` flag while open (6 minor markup issues, applied), rejected a garbled British-vowel objection, and rejected dropping `pleasant-adj` from `please-interj`'s word family (shared Latin root). Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.16.

## [2026-09-19] build | Twelve pronouns: anyone, anything, both, each, either, enough, everybody, everyone, everything, few, he, her

Sixth scheduled run, build chosen by scheduler debt. Pre-flight clean: no open pull request, no orphan branches, inbox empty. Drafted 12 band-1 pronouns continuing the determiner/pronoun pos-split set. Pipeline: validate, inflect, pronounce-check (24 transcriptions, all verified). Two-reviewer panel: 45 issues, 30 blocking; 30 applied, 15 rejected. Real catches: `each-pron`/`either-pron` definitions had comma splices or grammar notes folded in (a pattern across five entries — moved to the explanation); `each-pron`'s own definition used the headword circularly; its "ten dollars each"/"two cookies each" examples were postposed-adverbial, not pronoun, uses, and were removed; `everything-pron`/`anything-pron` definitions restated the headword as `any`/`every` plus `thing`; three learner-error and one collocation item illustrated the determiner use and were replaced. Rejected: three pronunciation objections against `anyone`, `everybody`, contradicted by the CMU/panel agreement already recorded; a length-mark objection on `few`'s American IPA against house style; two usage-note objections that would have contradicted the published, reviewed `both-det`/`few-det` entries. Gate, caps, links, 292 tests, lint_vocab and crossref gates pass. Spend: US$0.28.

## [2026-09-19] build | Ten determiners and four pronouns

Fifth scheduled run, chosen by scheduler debt (build highest). Pre-flight clean: no open pull request, no orphan branches, inbox empty. Claimed 20 slugs from the queue, then trimmed the batch to 14 (fewer, better entries) before drafting: determiners *that, their, these, this, those, what, whatever, which, whose, your* and pronouns *all, another, any, anybody*. Full pipeline run: validate, inflect (all verified), pronounce-check (28 transcriptions, all verified), two-reviewer panel (38 issues, 28 blocking). Adjudicated every blocking issue: 28 applied, 10 rejected. The two largest rejections were considered ones — kept *which one* and the *in which case / by which time* sense of `which-det` as determiner use (checked against a grammar reference: *which* directly precedes a noun there, `one`/`case`/`time`), against one reviewer's repeated claim that these are pronoun uses. Real catches: `whatever-det` and `all-pron` had drifted into their sibling pos-split entries' territory (clause use, noun-object "of" phrases) and were rewritten to stay noun-only / personal-pronoun-only respectively. Queued `these-pron` and `those-pron`, missing pos-split targets the existing queue rows for the set had not covered. Gate, caps, links, 292 tests, lint_vocab and crossref gates all pass. Spend: US$0.33.

## [2026-09-19] lint | Reviewer-precision switch-off; branch and claim cleanup

Fourth scheduled run, forced by the five-runs-since-last-lint rule. Pre-flight: no open pull request; two closed-by-owner branches show file residue against current `main` only because later runs kept editing the same entries — both already logged as owner-closed and absorbed by a different path, so no new curator lines; the other orphan branches confirmed fully absorbed (already logged). `claim.py --prune` removed four stale claim files.

`metrics.py --precision` over all 798 logged decisions: reviewer-a 0.56 overall, reviewer-b 0.37. Five (role, family) pairs stayed under 30 percent over twenty-plus decisions — reviewer-a `example-policy` (0.16/44), `grammar-code` (0.27/75), `sense-structure` (0.29/48); reviewer-b `example-policy` (0.05/83), `explanation` (0.24/21) — so all five are switched off in `tools/review_panel.py` (`DISABLED_FAMILIES`, three unit tests added): a flagged issue from these pairs is downgraded to `ok` before adjudication. Table and reasoning in [reviewer-precision](notes/reviewer-precision.md).

Mechanical checks: caps, links, `crossref --all --apply` (865 warnings, all expected missing closure targets), `lint_vocab --all --queue` (0 violations; noted two defining-vocabulary first senses using out-of-vocabulary words, `most-det`/`other-det`, left for a future review pass). Wiki index checked against pages on disk: no orphans, no dead links. Gate, caps, links, 292 unit tests pass. Spend: US$0.

## [2026-09-19] review | Draft entries reviewed; markup pending cleared on eight

Third scheduled run. Pre-flight: no open pull request; a merged branch confirmed absorbed and logged. Chose review mode. The four entries still draft since the founding session (can, child, each other, explain) got their missing reviewer-a pass and were promoted to reviewed; can gained a missing sense (impossibility, can't be true) and a corrected could/was-able-to contrast. Eight markup-pending entries (favorite, information, main, police, scissors, advice, afraid, asleep) got a second panel round, hand-added inline marks, and lost the flag; information's help-desk use moved from a miscoded subsense to its own countable sense, and asleep's etymology was corrected. Several reviewer-a objections (pronunciation, a grammar code) were rejected as wrong. 108 decisions logged, 101 applied. Reviewer precision: 0.875 / 0.944. Spend: US$0.33. 34 markup-pending entries remain; lint is due next run.

## [2026-09-18] build | Fourteen determiners: least, less, little, many, more, most, much, my, neither, no, other, our, several, such

Second scheduled Routine run. Pre-flight: no open pull request; one previously unlogged orphan branch (`claude/admiring-rubin-05mkcp`), fully absorbed, no residue, logged. `next_mode.py` chose build again. Claimed and drafted fourteen band-1 determiners, closing most of the core quantifier and possessive set. Both reviewers repeatedly caught one real problem: an "of" example or explanation (*several of the guests*, *many of the students*) mixing the pronoun/fused-head use into the determiner entry, against [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md); fixed throughout. Two misplaced idioms moved by the keyword rule: *once more* dropped (adverbial), *more or less* moved from **less** to **more**. Non-standard or incomplete collocation items were trimmed or completed (*no small* → *no small task*). Two double-negative usage notes were sharpened from "avoided" to "not standard English". Single-reviewer claims that would have broken the established comparative/superlative pattern across **less/least/more/most**, repeated the accepted **few**/**a few** ↔ **little**/**a little** structure, or relitigated the *either...or* / *at least* placement precedent were rejected with those reasons (109 decisions). **several**'s pronunciation stayed `disputed`; American settled by CMU, British flagged to the curator. Reviewer precision: 0.63 / 0.77. Spend: US$0.36.

## [2026-09-18] build | Ten determiners: both, each, either, enough, every, few, half, her, his, its

First scheduled Routine run. Pre-flight: `origin/main` had already absorbed the prior session's work; pull request #4's branch closed by the owner without an API merge but fully absorbed with no residue, logged in `reviews/needs_curator.txt`. Claimed and drafted ten determiners, band 1. Both reviewers caught real problems: several entries mixed the pronoun/adverb use of the same word into the determiner's examples and collocations (*each of us*, *either of them*), which [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md) reserves for a separate entry; American IPA carried stray length marks in three entries; two adaptation notes gave raw IPA instead of a plain respelling; a usage note asserted a false rule about **both** and a negative verb. Fixed and logged (59 decisions); a few single-reviewer, uncorroborated restructuring claims on **half** were rejected. *her*'s American pronunciation stayed `disputed`, siding with CMU over the panel; flagged. Nine pos-split pointers moved from `word_family` to `see_also` per the style guide, and queued. All ten pass the gate. Reviewer precision: 0.73 / 0.78. Spend: US$0.26.

## [2026-09-18] decision | Seed-set feedback: inline marks, part-of-speech discipline, plain pronunciation notes

Processed the owner's feedback on the seed set (`inbox/archive/20260918_feedback_on_seed_set.txt`), the first item to arrive in `inbox/`. Two decision pages: [inline-markup](decisions/inline-markup.md) (`**word**` for a word named as a word, `*phrase*` for a quoted illustration, the headword in examples marked by the site at build time) and [part-of-speech-and-consistency](decisions/part-of-speech-and-consistency.md) (one part of speech per entry, the entry agrees with itself, origin claims only in etymology, pronunciation notes in plain words). Tools: `eexlib`, `link_words`, `build_site`, `validate` (mark rules, an example without its headword, pronunciation jargon), `lint_vocab`, `originality_check`, `review_panel` (marks explained; a `consistency` verdict); 15 new unit tests; style guide section 6 and rules in sections 4, 7, 11, 12, 16; conventions; the Routine's review mode. Entries: 40 marked up by hand (the model entries, the function words, the drafts, the three the feedback named); 42 flagged `markup-pending` (a new flag value) for the review runs; *another*, *money*, *all*, *alone*, *favorite* corrected; seven pronunciation notes rewritten; *another* pron and *alone* adv queued. Open questions 4 (split *be*, *have*, *do*, *one*?) and 5 (the look of the marks). Site rebuilt, browser check 54 of 54. Spend: US$0.

## [2026-09-17] build | Founding session, Stages C and D: the seed set reviewed and published, the Routine dry-run

Both reviewers read all 82 seed entries (77 seed plus five determiners from the Routine dry run); the four smallest (*can*, *child*, *each other*, *explain*) got only one reviewer before the day's budget cap stopped the panel and stay `draft`. The session adjudicated 787 issues in 798 logged decisions (367 applied, 431 rejected, none escalated) and fixed the entries; 78 are `reviewed`. Reviewer precision measured for the first time ([note](notes/reviewer-precision.md)): 0.53 and 0.21, with three families below the switch-off line. The reviewer prompt's list of house conventions was extended; the style guide gained two rules (subsense restrictions live in the explanation; hedged claims about other languages). The site was built from the reviewed entries and passed the browser check (54 checks). A Sonnet-class dry run of `routine-prompt.md` drafted the five determiners and led to prompt fixes ([note](notes/routine-dry-run-v1.md)). Queue: 82 done, 4,195 pending. Spend on 2026-09-17: US$4.99 of 5.00, all reviews.

## [2026-09-16] setup | Founding session, Stage B: word lists, pronunciation test, pipeline and site tools

Built `schema/defining-vocabulary.txt` (2,394 lemmas) from three models' independent proposals in 37 semantic-field chunks, the intersection as the core, 682 single-vote words accepted one by one, gaps filled; `headwords/queue.tsv` with 3,907 rows (band 1 from the list, bands 2 and 3 by the same panel's median). Ran the pre-registered pronunciation test on 500 words and four models; adopted the two-vote rule (verified precision 95.0 percent) and amended the normalization for five notation conventions ([note](notes/pronunciation-model-test-v1.md), [decision](decisions/pronunciation-pipeline.md)). Wrote the exceptions table (checked by two models, [note](notes/inflection-exceptions-check.md)) and the tools `inflect`, `lint_vocab` (with a transparent-derivative rule), `pronounce_check`, `review_panel`, `crossref`, `queue`, `claim`, `next_mode`, `metrics`, `absorb_branch`, `originality_check`, `link_words`, `build_site`, `site_check`; `routine-prompt.md`; `tools/README.md`; the Pages workflow. Seed entries are drafted locally (77) and follow in Stage C. Spend so far today: US$4.75 of 5.00.

## [2026-09-16] setup | Founding session, Stage A: scaffold

Created the repository skeleton from the founding prompt: `framework.md` copied verbatim from the owner's knowledge-base framework; `PROJECT.md` (charter), `CLAUDE.md`, `resume-prompt.md`, `NEXT.md`, licences (data CC0 1.0, code MIT), README; `schema/entry.schema.json` and `schema/vocabularies.json` with the original usage-label sets; `wiki/` with conventions, index, this log, open questions, and 28 decision pages (two of them, defining-vocabulary and pronunciation-pipeline, to be completed in Stage B); `inbox/`, `journal/`, `reviews/`, `headwords/`, `metrics/`, `config/` (budget rule, ledger, model roles); tools `validate.py`, `schema_check.py`, `eexlib.py`, `entry_path.py`, `check_caps.py`, `check_links.py`, `spend.py`, `openrouter.py`, `wait.py` with unit tests; `.github/workflows/validate.yml`; a first version of `wiki/style-guide.md` (model entries to come from the seed set). Verified reachable and recorded in `sources/README.md`: OpenRouter's model list, the CMU Pronouncing Dictionary raw file, the Wiktionary API. Model roles chosen and verified against the OpenRouter list (`config/models.md`).
